/**
 * This file, though blank, is necessary for truffle's install and compile to function - 
 * even if they don't need it (which they don't)
 * TODO discuss with truffle team on their gitter what the roadmap is for install && compile
 * perhaps becoming their own standalone tools.
 */
